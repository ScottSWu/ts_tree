let x = 1;
let y = x as string;
let z = <string> x;

(5 + 3);

(((x + 2))[("toString")])()

let a = {
  "hello": "world" as string
};

let dateValue = <Date> (<any> source);
dateValue = <Date> (<any> source);
dateValue = <Date> <any> source;

function b(collection) {
  return new Set<string>(<string[]>collection);
  return <Language>((<any>Language)[key]) || Language.UNKNOWN;
}

this.errorItems_ = Array.isArray(errorItems) ?
  <Array<ErrorItem>>errorItems :
  [<ErrorItem>errorItems];

let c = 1 + 1 - 1 * 1 / 1 % 1;

export function createGapiRequestServiceModule(
    gapiUrl: string,
        {gapiKey = <string>'', gapiConfigUpdates = <{[key: string]: any}>{}}) {
}

enum ChartType {
  AREA_CHART = ('AreaChart' as any),
    BAR_CHART = ('BarChart' as any),
      DONUT_CHART = ('DonutChart' as any),
        GEO_CHART = ('GeoChart' as any),
          LINE_CHART = ('LineChart' as any),
            PIE_CHART = ('PieChart' as any),
              TABLE = ('Table' as any),
              }

